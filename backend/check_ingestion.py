import asyncio
from app.services.pipeline import PipelineRunner
from app.models.domain import ExecutionStatus

async def run_check():
    # just a script to check what is in the DB or run a fake pipeline
    pass
